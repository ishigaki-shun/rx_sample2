# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'rx_sample2' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for rx_sample2
  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'
  pod 'RxGesture'
  pod 'RxDataSources', '~> 3.0'
  pod 'RxKeyboard'

  target 'rx_sample2Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'rx_sample2UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
